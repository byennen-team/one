//Question: Not sure if I need to publish User again, since the users-elliman package should do this?

// Meteor.publish('user', function () {
//   if (! this.userId) return [];
//   return Meteor.users.find(this.userId);
// });
