Template.messageBoard.helpers({

// TODO: if current channel is a task return ture, else return false
  taskChannel: function () {
    return true;
  },

// TODO: if current channel is a room return ture, else return false
  roomChannel: function () {
    return false;
  }

});